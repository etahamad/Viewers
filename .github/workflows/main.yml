name: Docker Image CI

on:
  push:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build OHIF Viewer image via docker-compose
        run: |
          cd platform/app/.recipes/Nginx-Dcm4chee-Keycloak
          docker compose build ohif_viewer

      - name: Tag image
        run: |
          docker tag webapp:latest etahamad/ohif-viewer:v3.12.1-beta

      - name: Push image
        run: |
          docker push etahamad/ohif-viewer:v3.12.1-beta
